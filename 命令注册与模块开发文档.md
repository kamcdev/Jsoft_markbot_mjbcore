# 命令注册与模块开发文档

本文档详细说明硫酸钠BOT的命令注册机制与功能模块开发规范，涵盖 `group.json` 配置文件的各字段含义、模块文件结构、命令函数签名、消息拦截器、后台线程管理等内容。

---

## 目录

- [命令注册与模块开发文档](#命令注册与模块开发文档)
  - [目录](#目录)
  - [一、整体架构概览](#一整体架构概览)
  - [二、group.json 配置文件](#二groupjson-配置文件)
    - [2.1 commands（命令映射）](#21-commands命令映射)
    - [2.2 commandsinfo（命令简介）](#22-commandsinfo命令简介)
    - [2.3 commandscategory（命令分类）](#23-commandscategory命令分类)
    - [2.4 bot\_admin\_commands（Bot管理员命令）](#24-bot_admin_commandsbot管理员命令)
    - [2.5 group\_admin\_commands（群管理员命令）](#25-group_admin_commands群管理员命令)
    - [2.6 commandshidden（隐藏命令）](#26-commandshidden隐藏命令)
  - [2.7 完整注册示例](#27-完整注册示例)
  - [2.8 autoreg 模块自动注册（替代配置文件注册）](#28-autoreg-模块自动注册替代配置文件注册)
- [三、模块开发规范](#三模块开发规范)
    - [3.1 模块文件结构](#31-模块文件结构)
    - [3.2 modcfg() 模块元信息](#32-modcfg-模块元信息)
    - [3.3 命令函数签名](#33-命令函数签名)
    - [3.4 命令命名与自动注册](#34-命令命名与自动注册)
    - [3.5 init() 模块初始化](#35-init-模块初始化)
    - [3.6 模块配置文件读写](#36-模块配置文件读写)
  - [四、消息管线与扩展点](#四消息管线与扩展点)
    - [4.1 消息处理流程](#41-消息处理流程)
    - [4.2 消息拦截器](#42-消息拦截器)
    - [4.3 @bot 处理器](#43-bot-处理器)
    - [4.4 通知事件处理器](#44-通知事件处理器)
  - [五、后台线程管理](#五后台线程管理)
    - [5.1 启动后台线程](#51-启动后台线程)
    - [5.2 线程停止机制](#52-线程停止机制)
    - [5.3 临时线程注销](#53-临时线程注销)
  - [六、发送消息接口](#六发送消息接口)
  - [七、完整模块开发示例](#七完整模块开发示例)
    - [示例一：简单命令模块](#示例一简单命令模块)
    - [示例二：带后台线程的模块](#示例二带后台线程的模块)
    - [示例三：带拦截器的模块](#示例三带拦截器的模块)
  - [八、mjb.reload 重载机制](#八mjbreload-重载机制)
  - [九、注意事项与最佳实践](#九注意事项与最佳实践)
    - [文件编码](#文件编码)
    - [线程管理](#线程管理)
    - [主线程任务](#主线程任务)
    - [配置文件命名](#配置文件命名)
    - [日志使用](#日志使用)
    - [命令权限检查](#命令权限检查)
    - [文件路径](#文件路径)

---

## 一、整体架构概览

项目采用**编排层 + 核心层 + 模块层**的三层结构：

```
bot_v1_1.0.3.py          编排层：启动流程、动态加载模块、Webhook服务
├── bin/                  核心层（mjbcore）
│   ├── mjbconfig.py      配置读写
│   ├── message.py        消息解析与分发
│   ├── mjbc.py           命令注册与内置命令
│   ├── send.py           消息发送与LLbotQQ API封装
│   ├── worker.py         线程池与后台线程管理
│   ├── logger.py         日志与错误转发
│   ├── mjbmenu.py        帮助菜单生成
│   └── ...
└── modules/              模块层（功能命令）
    ├── __init__.py
    ├── ai.py
    ├── bilibili.py
    ├── hitokoto.py
    ├── filter.py
    └── ...
```

**模块加载流程**（由编排层 `load_modules()` 执行）：

1. 扫描 `modules/*.py`（排除 `__init__.py`）
2. 用 `importlib.import_module("modules.<name>")` 导入每个模块
3. 收集模块中所有 `cmd_` 开头的可调用函数，注册到命令分发表
4. 调用模块的 `init()`（若存在），注册拦截器、启动后台线程
5. 调用模块的 `modcfg()` 记录模块元信息

---

## 二、group.json 配置文件

`group.json` 是主配置文件，位于项目根目录。与命令注册相关的字段有六个，以下逐一说明。

### 2.1 commands（命令映射）

**作用**：定义"触发词 → 函数名"的映射。用户在群里发送的消息首词匹配到此表中的触发词时，会调用对应的函数。

**位置**：`group.json` 顶层 `"commands"` 对象。

**格式**：

```json
{
    "commands": {
        "触发词": "函数名",
        ...
    }
}
```

**说明**：
- **触发词**：用户实际发送的命令文本，如 `"ncc"`、`"mjb.reload"`、`"网易点歌"`。
- **函数名**：对应模块中定义的函数名。可以是短名（去掉 `cmd_` 前缀，如 `"ncc"`）或完整名（如 `"cmd_ai_deepseek"`）。编排层注册时会同时注册这两种名称。

**示例**：

```json
{
    "commands": {
        "mjb.help": "help",
        "mjb.reload": "reload",
        "mjb.mod": "mod",
        "ncc": "ncc",
        "ncc.play": "ncc_play",
        "网易点歌": "ncc",
        "ai.dpsk": "cmd_ai_dpsk",
        "hk.comic": "comic_hitokoto"
    }
}
```

上面 `"ncc": "ncc"` 表示用户发送 `ncc` 触发词，会调用 `modules/ncc.py` 中的 `cmd_ncc` 函数（注册时短名 `ncc` 指向该函数）。`"网易点歌": "ncc"` 则为中文别名，同样指向 `ncc` 函数。

### 2.2 commandsinfo（命令简介）

**作用**：为每个触发词提供一句话简介，显示在 `mjb.help` 帮助菜单中。

**位置**：`group.json` 顶层 `"commandsinfo"` 对象。

**格式**：

```json
{
    "commandsinfo": {
        "触发词": "简介文本",
        ...
    }
}
```

**示例**：

```json
{
    "commandsinfo": {
        "mjb.reload": "重载模块但不重启程序",
        "mjb.mod": "查看已载入模块",
        "ncc": "网易云点歌",
        "网易点歌": "网易云点歌",
        "ai.dpsk": "使用DeepSeek大模型回答问题"
    }
}
```

**注意**：键是**触发词**（与 `commands` 中的键一致），不是函数名。同一个函数若有多个触发词别名，每个别名都需单独配置简介。

### 2.3 commandscategory（命令分类）

**作用**：将触发词归类，帮助菜单按分类分组显示。未配置分类的命令归入"未分类"。

**位置**：`group.json` 顶层 `"commandscategory"` 对象。

**格式**：

```json
{
    "commandscategory": {
        "触发词": "分类名",
        ...
    }
}
```

**示例**：

```json
{
    "commandscategory": {
        "hk.comic": "一言",
        "hk.literature": "一言",
        "ncc": "la",
        "ncc.play": "la",
        "todayluck": "Games",
        "ai.dpsk": "AI",
        "python.corun": "Python",
        "qapi.ban": "QQ操作"
    }
}
```

**注意**：
- 管理员命令（在 `bot_admin_commands` 或 `group_admin_commands` 中的）会被单独提取到"管理员命令"分组，**无需**也**不应**在此配置分类。
- 仅需为普通命令配置分类。

### 2.4 bot_admin_commands（Bot管理员命令）

**作用**：声明仅Bot管理员可用的命令触发词列表。这些命令在帮助菜单中单独归入"管理员命令"分组，且只有Bot管理员可见。

**位置**：`group.json` 顶层 `"bot_admin_commands"` 数组。

**格式**：

```json
{
    "bot_admin_commands": [
        "触发词1",
        "触发词2",
        ...
    ]
}
```

**示例**：

```json
{
    "bot_admin_commands": [
        "mjb.reboot",
        "mjb.reload",
        "mjb.mod",
        "python.corun",
        "qapi.ban",
        "qapi.kick",
        "setluck",
        "mjb.botban",
        "mjb.botunban"
    ]
}
```

**注意**：列表中填入的是**触发词**（如 `"mjb.reload"`），不是函数名（如 `"reload"`）。

### 2.5 group_admin_commands（群管理员命令）

**作用**：声明仅群管理员（群主/管理员）可用的命令触发词列表。Bot管理员自动拥有群管理员权限。在帮助菜单中归入"管理员命令"分组，群管理员和Bot管理员可见。

**位置**：`group.json` 顶层 `"group_admin_commands"` 数组。

**格式**：与 `bot_admin_commands` 相同，为数组。

**示例**：

```json
{
    "group_admin_commands": [
        "mjb.autogpsing",
        "mjb.closeingp",
        "mjb.allowingp",
        "mjb.fk",
        "mjb.autogpauth"
    ]
}
```

### 2.6 commandshidden（隐藏命令）

**作用**：声明默认不在帮助菜单中显示的命令触发词。使用 `mjb.help -showhidden` 可显示全部隐藏命令。

**位置**：`group.json` 顶层 `"commandshidden"` 数组。

**格式**：

```json
{
    "commandshidden": [
        "触发词1",
        ...
    ]
}
```

**示例**：

```json
{
    "commandshidden": [
        "mjb.echo",
        "mjb.test"
    ]
}
```

### 2.7 完整注册示例

以下展示注册一个新命令 `"mytool"` 的完整配置（函数名 `mytool`，普通命令，分类"工具"）：

```json
{
    "commands": {
        "mytool": "mytool",
        "我的工具": "mytool"
    },
    "commandsinfo": {
        "mytool": "我的工具命令简介",
        "我的工具": "我的工具命令简介"
    },
    "commandscategory": {
        "mytool": "工具",
        "我的工具": "工具"
    },
    "bot_admin_commands": [],
    "group_admin_commands": [],
    "commandshidden": []
}
```

若该命令仅Bot管理员可用，则改为：

```json
{
    "commands": {
        "mytool": "mytool"
    },
    "commandsinfo": {
        "mytool": "我的工具命令简介"
    },
    "bot_admin_commands": [
        "mytool"
    ]
}
```

此时**无需**在 `commandscategory` 中配置（管理员命令会自动归入"管理员命令"分组）。

### 2.8 autoreg 模块自动注册（替代配置文件注册）

**作用**：模块可在 `modcfg()` 返回的字典中添加 `autoreg` 键，将命令注册表直接内置到模块代码中，加载模块时自动注册，**无需**修改 `group.json`。

**适用场景**：
- 模块作为独立功能单元分发，希望自带命令注册信息，即装即用
- 避免 group.json 因模块增删而频繁改动
- 模块开发者集中管理命令触发词、简介、分类等信息

**结构**：`autoreg` 的值是一个字典，结构与 `group.json` 中对应字段**完全一致**，支持以下键：

| 键 | 类型 | 说明 |
|----|------|------|
| `commands` | dict | 触发词 → 函数名 |
| `commandsinfo` | dict | 触发词 → 简介 |
| `commandscategory` | dict | 触发词 → 分类 |
| `bot_admin_commands` | list | Bot管理员命令触发词 |
| `group_admin_commands` | list | 群管理员命令触发词 |
| `commandshidden` | list | 隐藏命令触发词 |

**合并策略**：`group.json` 优先。若同一触发词/项在 `group.json` 中已配置，则 `autoreg` 中的对应项**不生效**。这样用户可在 `group.json` 中覆盖模块默认（改名、改简介、禁用、调整分类等）。

**示例**：模块 `modules/mytool.py` 自带命令注册

```python
def modcfg():
    return {
        "version": "1.0.0",
        "author": "JsoftStudio",
        "description": "我的工具模块",
        "autoreg": {
            "commands": {
                "mytool": "mytool",
                "我的工具": "mytool"
            },
            "commandsinfo": {
                "mytool": "我的工具命令简介",
                "我的工具": "我的工具命令简介"
            },
            "commandscategory": {
                "mytool": "工具",
                "我的工具": "工具"
            }
        }
    }
```

加载此模块后，即使 `group.json` 的 `commands` 中未写 `"mytool"`，用户发送 `mytool` 也能触发 `cmd_mytool` 函数，帮助菜单也会显示该命令及其简介、分类。

若用户希望在 `group.json` 中禁用该命令，可将触发词映射到不存在的函数名，或将触发词加入 `commandshidden`；若希望改名，在 `group.json` 的 `commands` 中用相同触发词键指向不同的函数名即可覆盖。

**与配置文件注册的关系**：
- 二者共存，运行时合并使用
- `autoreg` 提供"开箱即用"的默认注册
- `group.json` 提供"用户定制"的覆盖能力
- `mjb.reload` 重载时会重新合并（先重载 `group.json`，再重新加载模块并合并 `autoreg`）

---

## 三、模块开发规范

### 3.1 模块文件结构

每个功能模块是 `modules/` 目录下的一个 `.py` 文件，文件名即模块名。基本结构：

```python
# -*- coding: utf-8 -*-
"""modules/mytool.py - 我的工具模块

模块功能描述。
配置文件：mytool.json（通过 mjbconfig.load_module_config 读写）
"""
from bin import send, logger, mjbconfig, worker


def modcfg():
    """模块元信息"""
    return {
        "version": "1.0.0",
        "author": "YourName",
        "description": "模块功能描述",
    }


def cmd_mytool(group_id, user_id, config_data, *args):
    """我的工具命令"""
    # 命令逻辑
    send.group(group_id, "工具执行完成")
    return "执行成功"


def init():
    """模块初始化（可选）：注册拦截器、启动后台线程"""
    pass
```

**要点**：
- 文件头部使用 `# -*- coding: utf-8 -*-` 声明编码
- 文件docstring简述模块功能与配置文件
- 从 `bin` 包导入所需工具（`send`、`logger`、`mjbconfig`、`worker`、`message` 等）

### 3.2 modcfg() 模块元信息

每个模块**必须**定义 `modcfg()` 函数，返回包含模块元信息的字典：

```python
def modcfg():
    return {
        "version": "1.0.0",        # 模块版本
        "author": "JsoftStudio",   # 作者
        "description": "模块功能描述",  # 功能介绍
    }
```

此信息可通过 `mjb.mod info <模块名>` 命令查看。

**可选键 `autoreg`**：在 `modcfg()` 返回的字典中添加 `autoreg` 键，可将命令注册表内置到模块中，加载时自动注册，无需修改 `group.json`。详见 [2.8 autoreg 模块自动注册](#28-autoreg-模块自动注册替代配置文件注册)。

```python
def modcfg():
    return {
        "version": "1.0.0",
        "author": "JsoftStudio",
        "description": "模块功能描述",
        "autoreg": {                       # 可选：模块自带命令注册表
            "commands": {"mytool": "mytool"},
            "commandsinfo": {"mytool": "工具简介"},
            "commandscategory": {"mytool": "工具"},
        },
    }
```

### 3.3 命令函数签名

所有命令函数**必须**使用以下统一签名：

```python
def cmd_xxx(group_id, user_id, config_data, *args):
    """
    Args:
        group_id: 当前群号（字符串）
        user_id:  发送者QQ号（字符串）
        config_data: group.json 完整配置字典（实时）
        *args: 命令参数列表（已按空格分割，去除首词触发词）
    
    Returns:
        任意返回值（仅用于日志记录，不影响流程）
    """
    # 示例：处理参数
    if not args:
        send.group(group_id, "用法：mytool <参数>")
        return "未提供参数"
    
    param = args[0]
    send.group(group_id, f"收到参数：{param}")
    return f"已处理参数: {param}"
```

**参数说明**：

| 参数 | 类型 | 说明 |
|------|------|------|
| `group_id` | str | 当前群号 |
| `user_id` | str | 发送者QQ号 |
| `config_data` | dict | group.json 完整配置（实时读取，含commands/admin_list等） |
| `*args` | tuple | 命令参数，已按空格分割并去除首词触发词。如用户发送 `mytool a b c`，则 `args = ("a", "b", "c")` |

**返回值**：函数的返回值会被记录到日志（`命令执行结果: <返回值>`），但不影响消息处理流程。通常返回描述性字符串即可。

### 3.4 命令命名与自动注册

**命名规则**：命令函数必须以 `cmd_` 开头，如 `cmd_ncc`、`cmd_ai_dpsk`。

**自动注册**：编排层 `load_modules()` 会扫描模块中所有 `cmd_` 开头的可调用对象，并注册两个名称：

```python
# 假设模块定义了 def cmd_mytool(...):
# 注册时会同时注册：
#   "mytool"     -> cmd_mytool   （短名，去掉 cmd_ 前缀）
#   "cmd_mytool" -> cmd_mytool   （完整名）
```

因此在 `group.json` 的 `commands` 中，函数名既可写 `"mytool"` 也可写 `"cmd_mytool"`，两者都能匹配。**推荐使用短名**。

### 3.5 init() 模块初始化

模块可**可选**定义 `init()` 函数，在模块加载完成后由编排层调用。用途包括：

- 注册消息拦截器
- 注册 @bot 处理器
- 注册通知事件处理器
- 启动后台线程

```python
def init():
    """模块初始化：注册拦截器并启动后台线程"""
    # 注册拦截器
    message.register_interceptor(_my_interceptor)
    
    # 注册 @bot 处理器
    message.register_at_bot_handler(_my_at_bot_handler)
    
    # 启动后台线程
    worker.start_background(
        "mytool-worker",
        _background_task,
        stop_event=threading.Event(),
    )
    logger.info("mytool 模块初始化完成")
```

**注意**：`init()` 在模块加载时调用一次。执行 `mjb.reload` 时，编排层会先停止所有后台线程、清理拦截器，再重新加载所有模块并重新调用 `init()`。

### 3.6 模块配置文件读写

模块的私有配置文件统一存放在 `modules/config/` 目录。通过 `mjbconfig` 提供的接口读写：

```python
from bin import mjbconfig

CONFIG_FILE = "mytool.json"

def load_config():
    """加载模块配置，返回dict"""
    return mjbconfig.load_module_config(CONFIG_FILE) or {}

def save_config(config):
    """保存模块配置"""
    mjbconfig.save_module_config(CONFIG_FILE, config)
```

**接口说明**：

| 接口 | 说明 |
|------|------|
| `mjbconfig.load_module_config(name)` | 从 `modules/config/<name>` 读取JSON，文件不存在时回退到根目录，仍不存在返回 `{}` |
| `mjbconfig.save_module_config(name, data)` | 将dict写入 `modules/config/<name>`，自动创建目录 |

**主配置读写**（`group.json`）：

```python
# 读取group.json完整配置
config = mjbconfig.get_config()

# 读取单个字段
value = mjbconfig.get("some_key", default_value)

# 写回group.json（修改后需保存）
config["some_key"] = "new_value"
mjbconfig.save(config)
```

---

## 四、消息管线与扩展点

### 4.1 消息处理流程

群消息进入Webhook后，`message.handle_group_message()` 按以下顺序处理：

```
1. 消息拦截器（验证/屏蔽词/recall等）  ← 按注册顺序执行，返回True终止
2. @bot 检测                           ← 提取@内容
3. @bot 处理器（AI等）                  ← 命中@bot则执行，返回True终止
4. 黑名单检查                           ← 黑名单用户不进入后续流程
5. recall 引用撤回处理                  ← [CQ:reply,id=xxx]recall
6. 命令分发（mjbc.dispatch）            ← 首词匹配commands，调用对应函数
7. 关键词回复                           ← 未命中命令时检测gpevent关键词
```

**关键点**：
- 拦截器在最前，可拦截一切消息（含命令）
- @bot 消息不进入命令流程（除非@bot处理器返回False）
- 黑名单用户无法执行命令
- 命令首词匹配，剩余部分按空格分割为参数

### 4.2 消息拦截器

**用途**：在命令分发前对消息进行预处理，如屏蔽词检测、入群验证、字数限制等。拦截器返回 `True` 表示已处理并终止后续管线。

**注册方式**：在模块 `init()` 中调用 `message.register_interceptor(fn)`。

**拦截器函数签名**：

```python
def my_interceptor(ctx):
    """
    Args:
        ctx: 消息上下文字典，包含以下字段：
            - group_id: 群号
            - user_id: 发送者QQ
            - username: 发送者昵称
            - raw_message: 原始消息文本（含CQ码）
            - message_id: 消息ID
            - config_data: group.json配置
    Returns:
        True  - 已处理，终止后续管线（不进入命令/关键词流程）
        False - 未处理，继续后续流程
    """
    group_id = ctx["group_id"]
    raw_message = ctx["raw_message"]
    
    # 示例：检测特定内容
    if "屏蔽词" in raw_message:
        send.group(group_id, "消息包含敏感内容，已拦截")
        return True
    
    return False
```

**注册**：

```python
def init():
    message.register_interceptor(my_interceptor)
```

**执行顺序**：按各模块 `init()` 的注册顺序执行。模块加载顺序按 `modules/` 目录文件名排序。

### 4.3 @bot 处理器

**用途**：处理 @机器人 的消息，如AI对话。

**注册方式**：在模块 `init()` 中调用 `message.register_at_bot_handler(fn)`。

**处理器函数签名**：

```python
def my_at_bot_handler(ctx):
    """
    Args:
        ctx: 消息上下文，额外包含：
            - is_at_bot: True
            - at_content: 去除@后的消息内容
    Returns:
        True  - 已处理，终止后续流程
        False - 未处理，继续下一个@bot处理器
    """
    at_content = ctx["at_content"]
    group_id = ctx["group_id"]
    
    # 示例：回复@消息
    if at_content:
        send.group(group_id, f"收到你的消息：{at_content}")
        return True
    
    return False
```

**注意**：@bot 消息**不会**进入命令分发流程（步骤6）。所有@bot处理器执行完毕后直接返回。

### 4.4 通知事件处理器

**用途**：处理QQ通知事件，如群成员增加、好友申请等。

**注册方式**：在模块 `init()` 中调用 `message.register_notice_handler(notice_type, fn)`。

```python
def my_notice_handler(ctx):
    """处理群成员增加事件"""
    group_id = ctx.get("group_id")
    user_id = ctx.get("user_id")
    send.group(group_id, f"欢迎新成员 {user_id}")
    return True

def init():
    # 注册处理 "group_increase" 通知事件
    message.register_notice_handler("group_increase", my_notice_handler)
```

**常见通知类型**：`group_increase`（入群）、`group_decrease`（退群）、`group_ban`（禁言）、`friend_add`（加好友）等。

---

## 五、后台线程管理

所有后台线程**必须**通过 `worker` 模块统一管理，**禁止**直接使用 `threading.Thread` 创建裸线程。这确保 `mjb.reload` 时能正确停止所有线程。

### 5.1 启动后台线程

```python
import threading
from bin import worker

def init():
    # 方式一：使用 threading.Event 控制退出（推荐用于 while 循环）
    stop_event = threading.Event()
    worker.start_background(
        "mytool-worker",           # 线程名（唯一标识）
        _background_loop,          # 目标函数
        stop_event=stop_event,     # 停止事件（自动注入到函数kwargs）
    )
    
    # 方式二：使用 stop_attr 控制退出（用于类对象）
    monitor = MyMonitor()
    worker.start_background(
        "mytool-monitor",
        monitor,                   # 可调用对象（需实现__call__）
        stop_attr="stop",          # 调用 monitor.stop() 退出
    )
```

**`start_background` 参数说明**：

| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | str | 线程唯一标识，用于停止与管理 |
| `target` | callable | 线程目标函数或可调用对象 |
| `args` | tuple | 位置参数 |
| `kwargs` | dict | 关键字参数 |
| `daemon` | bool | 是否为守护线程（默认True） |
| `stop_attr` | str | 停止方法名（如 `"stop"`），用于对象式线程 |
| `stop_event` | Event | 停止事件，自动注入到target的kwargs（参数名 `stop_event`） |

### 5.2 线程停止机制

`mjb.reload` 执行时，`worker.stop_all_background()` 会按以下顺序尝试停止每个线程：

1. 若设置了 `stop_attr`：调用 `target.stop_attr()`（如 `monitor.stop()`），将对象内部标志位设为False
2. 若设置了 `stop_event`：调用 `stop_event.set()`，通知while循环退出
3. 等待线程自然退出（join，超时5秒）
4. 从登记表清理

**示例一：Event控制的循环线程**

```python
import threading
from bin import worker, logger

def _background_loop(stop_event):
    """后台循环任务"""
    while not stop_event.is_set():
        # 执行任务...
        logger.debug("执行后台任务")
        stop_event.wait(30)  # 等待30秒，或事件触发立即返回
    logger.info("后台任务线程已退出")

def init():
    stop_event = threading.Event()
    worker.start_background("mytool-loop", _background_loop, stop_event=stop_event)
```

**示例二：stop_attr控制的对象线程**

```python
from bin import worker, logger

class MyMonitor:
    def __init__(self):
        self.running = True
    
    def __call__(self):
        """线程入口"""
        while self.running:
            # 执行监控...
            pass
    
    def stop(self):
        """停止方法（对应 stop_attr="stop"）"""
        self.running = False

def init():
    monitor = MyMonitor()
    worker.start_background("mytool-monitor", monitor, stop_attr="stop")
```

### 5.3 临时线程注销

对于有限循环的临时线程（如登录轮询，执行N次后自然结束），线程结束时需主动从登记表注销，避免 `mjb.reload` 时尝试停止已结束的线程：

```python
from bin import worker, logger

def poll_login_status(group_id, key):
    """有限轮询线程，结束后自动注销"""
    try:
        for attempt in range(100):
            # 轮询逻辑...
            if login_success:
                break
            time.sleep(3)
    except Exception as e:
        logger.error(f"轮询失败: {e}")
    finally:
        # 无论正常结束还是异常，都注销线程登记
        worker.unregister_thread("mytool-poll")
```

---

## 六、发送消息接口

`bin/send.py` 封装了所有消息发送与LLbotQQ API调用：

```python
from bin import send

# 基础发送
send.group(group_id, "文本消息")                          # 发送群文本
send.group_at(group_id, user_id, " 回复内容")             # @某人并发送
send.group_reply(group_id, user_id, message_id, "内容")   # 引用回复
send.group_image(group_id, "image.png")                   # 发送图片
send.private(user_id, "私聊内容")                         # 发送私聊

# 合并转发
send.send_group_forward_msg(group_id, ["内容1", "内容2"], fake_name="标题")

# 群文件
send.send_group_file(group_id, "file.pdf", file_name="文件名")

# LLbotQQ API
send.get_group_member_role(group_id, user_id)   # 获取群成员身份(owner/admin/member)
send.delete_msg(message_id)                     # 撤回消息
send.set_group_kick(group_id, user_id)          # 踢出成员
send.set_group_ban(group_id, user_id, duration) # 禁言（秒）
send.send_like(user_id, times=10)               # 点赞
send.send_poke(group_id, user_id)               # 戳一戳
send.api(action, **payload)                     # 通用API调用
```

---

## 七、完整模块开发示例

### 示例一：简单命令模块

创建 `modules/echo.py`，提供回显命令：

```python
# -*- coding: utf-8 -*-
"""modules/echo.py - 回显命令模块"""
from bin import send, logger


def modcfg():
    return {
        "version": "1.0.0",
        "author": "JsoftStudio",
        "description": "回显用户输入的内容",
    }


def cmd_echo(group_id, user_id, config_data, *args):
    """回显命令：echo <内容>"""
    if not args:
        send.group(group_id, "用法：echo <要回显的内容>")
        return "未提供内容"
    
    text = " ".join(args)
    send.group(group_id, f"回显：{text}")
    logger.info(f"用户{user_id}在群{group_id}回显: {text}")
    return f"已回显: {text}"
```

**group.json 注册**：

```json
{
    "commands": {
        "echo": "echo"
    },
    "commandsinfo": {
        "echo": "回显输入内容"
    },
    "commandscategory": {
        "echo": "MarkBOT"
    }
}
```

### 示例二：带后台线程的模块

创建 `modules/reminder.py`，提供定时提醒命令：

```python
# -*- coding: utf-8 -*-
"""modules/reminder.py - 定时提醒模块

配置文件：gpreminder.json（通过 mjbconfig.load_module_config 读写）
"""
import threading
from datetime import datetime

from bin import send, logger, mjbconfig, worker

# 模块级停止事件
_stop_event = None


def modcfg():
    return {
        "version": "1.0.0",
        "author": "JsoftStudio",
        "description": "定时提醒：每分钟检查并推送到期提醒",
    }


def _load_config():
    return mjbconfig.load_module_config("gpreminder.json") or {}


def _save_config(config):
    mjbconfig.save_module_config("gpreminder.json", config)


def _reminder_loop(stop_event):
    """提醒检查循环"""
    while not stop_event.is_set():
        try:
            now = datetime.now()
            current_time = now.strftime("%H:%M")
            config = _load_config()
            
            for group_id, reminders in config.items():
                for reminder in reminders:
                    if reminder.get("time") == current_time:
                        send.group(group_id, f"提醒：{reminder.get('content', '')}")
        except Exception as e:
            logger.error(f"提醒检查失败: {e}")
        
        stop_event.wait(60)  # 每分钟检查一次
    
    logger.info("提醒线程已退出")


def cmd_setreminder(group_id, user_id, config_data, *args):
    """设置提醒：setreminder <时间> <内容>
    例：setreminder 14:30 开会
    """
    if len(args) < 2:
        send.group(group_id, "用法：setreminder <HH:MM> <内容>")
        return "参数不足"
    
    time_str = args[0]
    content = " ".join(args[1:])
    
    config = _load_config()
    config.setdefault(str(group_id), []).append({
        "time": time_str,
        "content": content,
    })
    _save_config(config)
    
    send.group(group_id, f"已设置提醒：{time_str} - {content}")
    return "提醒已设置"


def init():
    """启动提醒后台线程"""
    global _stop_event
    _stop_event = threading.Event()
    worker.start_background("reminder-worker", _reminder_loop, stop_event=_stop_event)
    logger.info("提醒模块初始化完成")
```

**group.json 注册**：

```json
{
    "commands": {
        "setreminder": "setreminder"
    },
    "commandsinfo": {
        "setreminder": "设置定时提醒"
    },
    "commandscategory": {
        "setreminder": "工具"
    }
}
```

### 示例三：带拦截器的模块

创建 `modules/antiflood.py`，提供刷屏检测拦截器：

```python
# -*- coding: utf-8 -*-
"""modules/antiflood.py - 刷屏检测拦截器

检测用户短时间内连续发送多条消息，触发警告。
"""
import time
from collections import defaultdict

from bin import send, logger, message

# 消息记录：{group_id: {user_id: [timestamp, ...]}}
_msg_records = defaultdict(lambda: defaultdict(list))
# 警告记录：{group_id: {user_id: warning_count}}
_warning_counts = defaultdict(lambda: defaultdict(int))

# 配置
FLOOD_THRESHOLD = 5      # 连续消息数阈值
FLOOD_WINDOW = 10        # 时间窗口（秒）
MAX_WARNINGS = 3         # 最大警告次数


def modcfg():
    return {
        "version": "1.0.0",
        "author": "JsoftStudio",
        "description": "刷屏检测拦截器：检测并警告连续发送消息的用户",
    }


def _antiflood_interceptor(ctx):
    """刷屏检测拦截器"""
    group_id = str(ctx["group_id"])
    user_id = str(ctx["user_id"])
    now = time.time()
    
    # 清理过期记录
    records = _msg_records[group_id][user_id]
    _msg_records[group_id][user_id] = [t for t in records if now - t < FLOOD_WINDOW]
    
    # 添加当前消息时间戳
    _msg_records[group_id][user_id].append(now)
    
    # 检查是否超过阈值
    if len(_msg_records[group_id][user_id]) > FLOOD_THRESHOLD:
        _warning_counts[group_id][user_id] += 1
        warning_count = _warning_counts[group_id][user_id]
        
        if warning_count >= MAX_WARNINGS:
            # 超过最大警告次数，撤回并提示
            send.group(group_id, f"用户{user_id}因刷屏已被警告{warning_count}次")
        else:
            send.group(group_id, f"用户{user_id}请勿刷屏（警告{warning_count}/{MAX_WARNINGS}）")
        
        # 清空记录，重新计数
        _msg_records[group_id][user_id] = []
        return True  # 拦截此消息
    
    return False


def init():
    """注册刷屏检测拦截器"""
    message.register_interceptor(_antiflood_interceptor)
    logger.info("刷屏检测拦截器已注册")
```

**group.json 注册**：

此模块无命令，仅需在 `modules/` 目录创建文件即可。编排层加载时会自动调用 `init()` 注册拦截器。无需修改 `group.json` 的 `commands` 等字段。

```json
{}
```

---

## 八、mjb.reload 重载机制

`mjb.reload` 命令（仅Bot管理员）可重载所有模块而不重启程序。执行流程：

```
1. stop_all_background(wait=True, timeout=5)   # 停止所有后台线程并等待退出
2. message.clear_handlers()                    # 清空拦截器/@bot处理器/通知处理器
3. mjbc.clear_commands()                       # 清除非内置命令（保留help/test/echo等）
4. 从 sys.modules 卸载所有 modules.*           # 释放旧模块
5. load_modules()                              # 重新扫描加载模块，注册命令，调用init()
6. 重新自检并加载配置
```

**模块开发注意事项**：

1. **后台线程必须可停止**：使用 `stop_event` 或 `stop_attr`，确保线程能在5秒内退出。`while True` + `time.sleep` 的线程**无法**被停止，会导致reload后线程重复创建。

2. **避免模块级单例状态**：reload会重新导入模块，模块级变量会被重置。若需持久化状态，写入配置文件。

3. **init() 可重入**：`init()` 在每次reload时都会被调用，确保其中逻辑可重复执行（如启动线程前检查是否已运行）。

4. **拦截器会自动清理**：无需在模块中手动注销拦截器，`clear_handlers()` 会统一清空。

---

## 九、注意事项与最佳实践

### 文件编码

所有Python文件使用UTF-8编码，文件头部声明 `# -*- coding: utf-8 -*-`。

### 线程管理

- **禁止**直接使用 `threading.Thread` 创建裸线程，必须通过 `worker.start_background()` 注册
- **禁止**在命令函数中使用 `while True` 无限循环（会阻塞线程池）
- 耗时操作（如网络请求）在命令函数中直接执行即可，命令本身已通过线程池异步处理

### 主线程任务

某些操作必须在主线程执行（如 `pywebview.start()`），通过以下方式调度：

```python
from bin import socket

def cmd_screenshot(group_id, user_id, config_data, *args):
    """网页截图命令（需主线程执行webview）"""
    # 通过run_on_main_thread调度到主线程
    socket.run_on_main_thread(lambda: _do_screenshot(group_id, url))
    send.group(group_id, "正在截图...")
```

### 配置文件命名

- 模块私有配置：`gp<功能>.json`，存放在 `modules/config/`，如 `gpsign.json`、`blive.json`
- 核心配置（banuser/gpevent/gpauths）：保留在项目根目录

### 日志使用

```python
from bin import logger

logger.info("普通信息")
logger.warning("警告信息")
logger.error("错误信息")
logger.debug("调试信息")
```

`logger.error()` 的内容会自动转发到主群（通过错误通知机制）。

### 命令权限检查

内置命令（如 `cmd_reload`、`cmd_mod`）在函数内部自行检查权限。模块命令若需限制权限，同样在函数内检查：

```python
from bin import mjbutils, send

def cmd_admin_only(group_id, user_id, config_data, *args):
    """仅Bot管理员可用的命令"""
    admin_list = mjbutils.get_admin_list_from_config(config_data)
    if str(user_id) not in admin_list:
        send.group(group_id, "权限不足，仅Bot管理员可用")
        return "权限不足"
    
    # 命令逻辑...
```

**推荐做法**：将命令触发词加入 `group.json` 的 `bot_admin_commands` 或 `group_admin_commands`，帮助菜单会按权限自动过滤显示。但**注意**：这两个列表仅控制菜单可见性，**不**阻止用户执行。若需强制权限校验，仍需在函数内检查。

### 文件路径

- 字体、图片等资源文件最好放在Bot工作目录内，且使用**相对路径**（如 `"misans.ttf"`），避免使用含盘符的绝对路径
- Windows下FFmpeg drawtext滤镜对含冒号的绝对路径解析异常
